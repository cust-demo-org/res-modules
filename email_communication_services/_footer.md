## Cross-Module References

This module exposes `email_communication_services` and `email_services_domains` outputs. The `communication_services` module references email domains (keyed `<service_key>|<domain_key>`) via its `linked_domains.keys`.
