## Cross-Module References

This module's output (`web_sites`) can be consumed by other modules in this repository. It references the `app_service_plan` and `application_insights` modules via their map keys.
