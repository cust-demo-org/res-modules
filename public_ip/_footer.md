## Cross-Module References

This module's output (`public_ips`) can be consumed by other modules in this repository. The `application_gateway` and `api_management_service` modules reference public IPs via their map keys.
