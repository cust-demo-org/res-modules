## Cross-Module References

This module's output (`web_application_firewall_policies`) can be consumed by other modules in this repository. The `application_gateway` module references WAF policies via `app_gateway_waf_policy_key`.
