## Cross-Module References

This module's output (`application_gateways`) can be consumed by other modules in this repository. It references the `public_ip` and `web_application_firewall_policy` modules via their map keys.
